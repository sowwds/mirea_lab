<?php

/**
 * Rate Limiter Configuration
 * 
 * Настройки rate limiting с использованием Redis для распределенного монолита
 */

return [
    /*
    |--------------------------------------------------------------------------
    | Rate Limiter Stores
    |--------------------------------------------------------------------------
    |
    | Здесь вы можете настроить хранилища для rate limiting.
    | По умолчанию используется Redis для распределенного доступа.
    |
    */

    'stores' => [
        'redis' => [
            'driver' => 'redis',
            'connection' => 'default',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Default Rate Limiter
    |--------------------------------------------------------------------------
    |
    | По умолчанию используется Redis для rate limiting
    |
    */

    'default' => env('RATE_LIMITER_STORE', 'redis'),

    /*
    |--------------------------------------------------------------------------
    | Rate Limit Prefix
    |--------------------------------------------------------------------------
    |
    | Префикс для ключей в Redis
    |
    */

    'prefix' => env('RATE_LIMITER_PREFIX', 'rate_limit:'),
];

